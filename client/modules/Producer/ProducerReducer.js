// Import Actions
import {} from './ProducerActions';

// Initial State
const initialState = {};

const ProducerReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default ProducerReducer;
