// Import Actions
import {  } from './OrderActions';

// Initial State
const initialState = {};

const OrderReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default OrderReducer;
